本次实验主题为问答模型：
通过使用SQuAD数据集微调DistilBERT模型，来进行推测。
所以还是需要首先下载对应的数据集squad，并根据模型的token化进行相应的词向量化，不过对于文本可能会出现过长长短不一等问题，因此需要对数据进行padding以及阶段等操作。同时对于训练数据集，要正确使用起始位置去对应文本。
接下来就是熟悉的下载模型，并进行微调，然后将模型送入huggingface，对于完整模型通过自己写的实例，可以查看各种输出数值，同时，可以适当调整输出，只保留关键文本（可以用于拓展到封装成专用机器人）